<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!--
    // COMPLETED (2) Creatmainmenu resource in mainmenu/ called main.xml
    // COMPLETED (3) Add one item to mainmenu with an ID of action_refresh
    // COMPLETED (4) Set the title of mainmenu item to "Refresh" using strings.xml
    // COMPLETED (5) Set the orderInCategory value to 1 to make sure this item is the first in the list
    // COMPLETED (6) Set app:showAsAction to ifRoom to display mainmenu item in the ActionBar if there is room
    -->
    <item
        android:id="@+id/action_refresh"
        android:orderInCategory="1"
        android:title="@string/refresh"
        app:showAsAction="ifRoom"></item>

</menu>